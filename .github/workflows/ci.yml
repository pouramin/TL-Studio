name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install UI build tools
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Type-check browser UI
        run: npm run check:web
      - name: Build browser UI
        run: npm run build:web
      - name: Test
        run: go test ./...
      - name: Vet
        run: go vet ./...
      - name: Verify browser JavaScript syntax
        run: |
          for file in cmd/launcher/web/*.js; do node --check "$file"; done
      - name: Verify npx quick launcher
        run: |
          node --check scripts/npx-launch.cjs
          npm pack --dry-run --ignore-scripts
      - name: Enforce TL Studio runtime boundary
        shell: bash
        run: |
          set -euo pipefail
          files=(cmd/launcher/web/*.js)
          if grep -nE '/kilo|Kilo backend|Sign in to Kilo|Kilo account' "${files[@]}" --exclude=runtime-api.js; then
            echo "Browser UI must not depend on or expose the bundled runtime implementation." >&2
            exit 1
          fi
          if grep -nE '/kilo' cmd/launcher/ui/*.ts --exclude=runtime-api.ts; then
            echo "TypeScript UI modules must access the bundled engine only through runtime-api.ts." >&2
            exit 1
          fi
      - name: Verify Python test syntax
        run: |
          python3 -m py_compile \
            scripts/check-kilo-product-contract.py \
            scripts/check-kilo-project-routing.py \
            scripts/fake-openai-server.py \
            scripts/check-kilo-prompt-e2e.py
      - name: Cross-compile supported launchers
        shell: bash
        run: |
          set -euo pipefail
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o /tmp/tl-studio.exe ./cmd/launcher
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /tmp/tl-studio-linux-x64 ./cmd/launcher
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o /tmp/tl-studio-linux-arm64 ./cmd/launcher
          CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -o /tmp/tl-studio-darwin-x64 ./cmd/launcher
          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -o /tmp/tl-studio-darwin-arm64 ./cmd/launcher

  runtime-product-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - name: Build launcher and download pinned runtime
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/tl-contract/bin /tmp/tl-runtime-upstream
          go build -trimpath -o /tmp/tl-contract/tl-studio ./cmd/launcher
          KILO_VERSION="$(tr -d '[:space:]' < KILO_VERSION)"
          ASSET="kilo-linux-x64.tar.gz"
          curl --fail --location --retry 3 \
            "https://github.com/Kilo-Org/kilocode/releases/download/v${KILO_VERSION}/${ASSET}" \
            -o "/tmp/tl-runtime-upstream/${ASSET}"
          tar -xzf "/tmp/tl-runtime-upstream/${ASSET}" -C /tmp/tl-runtime-upstream
          KILO_BIN="$(find /tmp/tl-runtime-upstream -type f -name kilo | head -n 1)"
          test -n "$KILO_BIN"
          cp "$KILO_BIN" /tmp/tl-contract/bin/kilo
          chmod +x /tmp/tl-contract/tl-studio /tmp/tl-contract/bin/kilo
      - name: Run real-runtime product API contract test
        shell: bash
        run: |
          set -euo pipefail
          LOG="$(mktemp)"
          START_DIR="$PWD"
          cd /tmp
          /tmp/tl-contract/tl-studio --no-browser --project "$GITHUB_WORKSPACE" >"$LOG" 2>&1 &
          PID=$!
          cd "$START_DIR"
          cleanup() {
            kill "$PID" 2>/dev/null || true
            wait "$PID" 2>/dev/null || true
            echo "--- launcher log ---"
            cat "$LOG"
          }
          trap cleanup EXIT

          LOCAL_URL=""
          for _ in $(seq 1 100); do
            LOCAL_URL="$(awk '/Local:/ {print $2; exit}' "$LOG" || true)"
            if [[ -n "$LOCAL_URL" ]] && curl --fail --silent "$LOCAL_URL/runtime/global/health" >/dev/null; then
              break
            fi
            sleep 0.2
          done
          test -n "$LOCAL_URL"
          python3 scripts/check-kilo-project-routing.py "$LOCAL_URL"
          python3 scripts/check-kilo-product-contract.py "$LOCAL_URL"

  runtime-product-prompt-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - name: Build launcher and download pinned runtime
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/tl-e2e/bin /tmp/tl-runtime-upstream
          go build -trimpath -o /tmp/tl-e2e/tl-studio ./cmd/launcher
          KILO_VERSION="$(tr -d '[:space:]' < KILO_VERSION)"
          ASSET="kilo-linux-x64.tar.gz"
          curl --fail --location --retry 3 \
            "https://github.com/Kilo-Org/kilocode/releases/download/v${KILO_VERSION}/${ASSET}" \
            -o "/tmp/tl-runtime-upstream/${ASSET}"
          tar -xzf "/tmp/tl-runtime-upstream/${ASSET}" -C /tmp/tl-runtime-upstream
          KILO_BIN="$(find /tmp/tl-runtime-upstream -type f -name kilo | head -n 1)"
          test -n "$KILO_BIN"
          cp "$KILO_BIN" /tmp/tl-e2e/bin/kilo
          chmod +x /tmp/tl-e2e/tl-studio /tmp/tl-e2e/bin/kilo
      - name: Run product prompt and real write-tool E2E
        shell: bash
        run: |
          set -euo pipefail
          FAKE_LOG="$(mktemp)"
          LAUNCHER_LOG="$(mktemp)"
          PROJECT_DIR="/tmp/tl-e2e-project"
          rm -rf "$PROJECT_DIR"
          mkdir -p "$PROJECT_DIR"

          FAKE_WRITE_PATH="$PROJECT_DIR/hello.txt" python3 scripts/fake-openai-server.py >"$FAKE_LOG" 2>&1 &
          FAKE_PID=$!
          LLM_URL=""
          for _ in $(seq 1 50); do
            LLM_URL="$(head -n 1 "$FAKE_LOG" || true)"
            [[ "$LLM_URL" == http://127.0.0.1:* ]] && break
            sleep 0.1
          done
          [[ "$LLM_URL" == http://127.0.0.1:* ]]

          # Mirror Kilo v7.6.2's own production test-provider fixture exactly:
          # singular `provider`, @ai-sdk/openai-compatible, options.baseURL.
          python3 - "$LLM_URL" "$PROJECT_DIR/opencode.json" <<'PY'
          import json, sys
          url, output = sys.argv[1], sys.argv[2]
          config = {
              "formatter": False,
              "lsp": False,
              "provider": {
                  "test": {
                      "name": "Test",
                      "id": "test",
                      "env": [],
                      "npm": "@ai-sdk/openai-compatible",
                      "models": {
                          "test-model": {
                              "id": "test-model",
                              "name": "Test Model",
                              "attachment": False,
                              "reasoning": False,
                              "temperature": False,
                              "tool_call": True,
                              "release_date": "2025-01-01",
                              "limit": {"context": 100000, "output": 10000},
                              "cost": {"input": 0, "output": 0},
                              "options": {}
                          }
                      },
                      "options": {"apiKey": "test-key", "baseURL": url}
                  }
              }
          }
          with open(output, "w", encoding="utf-8") as f:
              json.dump(config, f)
          PY

          unset KILO_CONFIG_CONTENT || true
          START_DIR="$PWD"
          cd /tmp
          /tmp/tl-e2e/tl-studio --no-browser --project "$PROJECT_DIR" >"$LAUNCHER_LOG" 2>&1 &
          LAUNCHER_PID=$!
          cd "$START_DIR"

          cleanup() {
            kill "$LAUNCHER_PID" 2>/dev/null || true
            kill "$FAKE_PID" 2>/dev/null || true
            wait "$LAUNCHER_PID" 2>/dev/null || true
            wait "$FAKE_PID" 2>/dev/null || true
            echo "--- launcher log ---"
            cat "$LAUNCHER_LOG"
            echo "--- fake LLM log ---"
            cat "$FAKE_LOG"
            echo "--- project config ---"
            cat "$PROJECT_DIR/opencode.json"
            echo "--- project files ---"
            find "$PROJECT_DIR" -maxdepth 2 -type f -print -exec sh -c 'echo "--- $1"; cat "$1"' _ {} \;
          }
          trap cleanup EXIT

          LOCAL_URL=""
          for _ in $(seq 1 100); do
            LOCAL_URL="$(awk '/Local:/ {print $2; exit}' "$LAUNCHER_LOG" || true)"
            if [[ -n "$LOCAL_URL" ]] && curl --fail --silent "$LOCAL_URL/runtime/global/health" >/dev/null; then
              break
            fi
            sleep 0.2
          done
          test -n "$LOCAL_URL"
          python3 scripts/check-kilo-project-routing.py "$LOCAL_URL"
          python3 scripts/check-kilo-prompt-e2e.py "$LOCAL_URL"
